clear  all;
load("ChladniE.m");
load("ChladniA.m");
load("ChadniOct.m");

sizeMat = size(ChladniA);
side = sqrt(sizeMat(1));

E = reshape(ChaldniE,side,side);
A = reshape(ChladniA,side,side);
Oct = reshape(ChladniOct,side,side);

figE = imagesc(E);
figA = imagesc(A);
figOct = imagesc(Oct);
